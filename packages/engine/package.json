{
  "name": "@previsao/engine",
  "version": "0.1.0",
  "description": "Motor puro do jogo Previsão: regras, vazas e pontuação. Sem UI, sem rede.",
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vitest": "^2.1.8"
  }
}
